FUSEKI (antes de la API)
- Apache Jena Fuseki en puerto 3030, dataset: cafe
- Importar CafeV9_Final.rdf una vez en Fuseki si el dataset está vacío
- URLs por defecto: http://localhost:3030/cafe/query y http://localhost:3030/cafe/update 

CORRER LA API
cd "C:\Users\froml\Downloads\Zunga (2) (1)\Zunga\cafeAPI"
python -m venv .venv-nuevo
.\.venv-nuevo\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
uvicorn main:app --reload --port 8001

CORRER EL DJANGO

cd "C:\Users\froml\Downloads\Zunga (2) (1)\Zunga\proyectoCafe\proyectoCafe"
python -m venv .venv-nuevo
.\.venv-nuevo\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 8000